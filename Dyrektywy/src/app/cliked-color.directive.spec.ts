import { ClikedColorDirective } from './cliked-color.directive';

describe('ClikedColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ClikedColorDirective();
    expect(directive).toBeTruthy();
  });
});
