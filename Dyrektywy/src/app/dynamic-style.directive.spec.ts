import { DynamicStyleDirective } from './dynamic-style.directive';

describe('DynamicStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicStyleDirective();
    expect(directive).toBeTruthy();
  });
});
